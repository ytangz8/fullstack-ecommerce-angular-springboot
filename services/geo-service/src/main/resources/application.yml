server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: geo-service
  datasource:
    url: jdbc:h2:mem:geodb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    defer-datasource-initialization: true
  data:
    rest:
      base-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info
